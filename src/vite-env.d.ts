/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/**
 * Sample-category filenames discovered in public/categories at build time and
 * injected via Vite's `define` (see vite.config.ts).
 */
declare const __SAMPLE_CATEGORY_FILES__: readonly string[];
